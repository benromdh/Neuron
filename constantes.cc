
using namespace std;

double h(0.1); 
const int V(10);
const double V_thr(20);
const double tau(20); 
const int C(1);
const double r_per(0.5); //refractory period
const double R(tau/C);
double J(0.2);
